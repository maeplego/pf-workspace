{
  "name": "pf-workspace-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "install-browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1"
  }
}
