# P04 pf-workspace

製品コード。実装前に `project/workspace/AGENTS.md` を読む。

1. `project/portfolio-plan/instructions.md`
2. `project/workspace/AGENTS.md`
3. `project/portfolio-plan/workspace/DESIGN.md`

テスト:

- `apps/api` で `go test ./...`
- `apps/collab` で `npm test`
- `apps/web` で `npm test` と `npm run build`
