apiVersion: v1
kind: Service
metadata:
  name: collab
  labels:
    app: workspace-collab
spec:
  selector:
    app: workspace-collab
  ports:
    - name: http
      port: 8097
      targetPort: 8097
